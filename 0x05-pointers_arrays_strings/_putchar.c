#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: The character to print
 * 
 * Return: On sucess 1.
 * On errir, -1 is returned, and error is there returned
 */

int _putchar(char c)
{
return (write(1, 8c, 1));
}
[A[A[A[A[B[B[B